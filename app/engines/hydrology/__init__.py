"""Hydrology engines: conditioning, D8 routing, accumulation, streams, catchment.

Pure numpy. Every function takes and returns arrays or :class:`Raster`
objects; nothing here reads a file, opens a socket or knows about a job.
"""
