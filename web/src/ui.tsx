/* The app's UI primitives come from the design-system package (web/ds, "pond-planner-ui").
   Vite compiles its source directly; `npm run build` in web/ds emits the dist/ that
   the AI design tool consumes via the design-sync tooling. */
export * from "../ds/src/index";
